name: Node CI

on: [pull]

jobs:
  build_and_test:
    runs-on: ubuntu-latest
    steps:
      - name: Upload Artifact
        uses: actions/upload-artifact@v2
        with:
          name: my-artifact
          path: my_file.txt